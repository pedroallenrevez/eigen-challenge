pre-commit:
  parallel: true
  commands:
    treefmt:
      run: treefmt
    ruff:
      run: poetry run ruff .
    # mypy:
    #   run: poetry run mypy . --ignore-missing-imports --install-types --non-interactive